#include "holberton.h"
/**
 * reverse_array - a function 
 * @a: pointer.
 * @n: variable.
 * Return: void.
 */
void reverse_array(int *a, int n)
{
int c = 0;
int b;

while (c < (n / 2))
{
t = a[c];
a[c] = a[n - c - 1];
a[n - c - 1] = bnew ;
c++;
}


