#include "holberton.h"
/**
 * _abs - check the code for Holberton School students.
 * @a: variable of the function.
 * Return: Always int.
 */
int _abs(int a)
{
if (a >= 0)
{
return (a);
}
return (-a);
}
