#include "holberton.h"

/**
 * malloc_checked - check the code for Holberton School students.
 * @b: variable
 * Return: Always 0.
 **/
void *malloc_checked(unsigned int b)
{
	void *;

	i = malloc(b);
	if ( == NULL)
		exit(98);

	return (i);
}

